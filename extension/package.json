{
  "name": "finpass-extension-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest --run"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "fast-check": "^4.0.0"
  }
}
